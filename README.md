## Папки Task и Solution
- Первая папка Task это сырой материал который вам нужно будет сделать самостоятельно
- Вторая папка Solution там полностью решенный материал, мы можете подсматривать если чтото не понятно
- Так же я в каждом задание буду в README писать подсказки чтобы вам было проще по этому когда заходите в папку в GitHub сначала ОБЯЗАТЕЛЬНО прочитайте информацию а затем идите делать.
- Так же если вы хотите делать каждую тему вы сначала заходите в GitHub читаете readme а затем открываете Task и уже делаете задания, есть трудности заходите в Solution



# Приветствую, здесь будет гайд по репозиторию. Как его легко клонировать через Git.



## Шаг первый скачайте Git.

- Вы заходите на сайт [Git](https://git-scm.com/install/)
- Скачиваете на свое устройсто, виндовс, мак и тд
- В установщике ничего не меняем просто нажимаем next
- Потом заходите в папку Git куда вы скачали
- Ищите `git-bash.exe` открываете и вот у вас Git

## Шаг второй настраиваем Git.

 ```bash
 git config --global user.name "ИмяВаше"
```
 ```bash
 git config --global user.email "имейл@gmail.com"
```
- Это нужно, чтобы 'Git знал, кто делает изменения
- Действует для всех репозиториев на этом компьютере

Проверяем настроилось ли:

- Пришем ` git config --list`
- Почти в самом низу ищем `user.name` и `user.email`
- Должно быть ваши имена и имейлы
- Если есть вы все правильно делаете

## Шаг третий настрой VS Code как редактор

Если хочешь, чтобы Git открывал VS Code при необходимости редактировать коммиты:
```bash
- git config --global core.editor "code --wait"
```

## Всё готово к работе.

Клонировать репозиторий курса:

- Выбираете папку в которую вам нужно скопировать репозиторий.
- это можно сделать командой
- `cd путь/до/папки`
- В моем случае `/c/Python`
- Если у вас название папки больше одного слова вам нужно взятьв кавычки cd "/c/Python practic"
- Затем пишем `git clone https://github.com/NemirovskyMax/Practice.git` на мой репозиторий.

И все у вас теперь заходи в VS Code и там ищем эту папку.

## Важно запомнить навсегда

После клонирования моего репозитория, вам чтобы обновить и добавить мои новые файлы, нужно быть в папке которой у вас лежит репозиторий в моем случае `/c/Python`.
И пишем туда `git pull` и все обновления у вас и можете приступать к работе.
